# 📋 Инструкция по деплою DEX контрактов

## Быстрая навигация

- 🚀 **[QUICK_DEPLOY_GUIDE.md](QUICK_DEPLOY_GUIDE.md)** - Быстрый старт (5 минут)
- 📚 **[DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)** - Полное руководство со всеми деталями
- ⚡ Команды ниже - для опытных пользователей

## Структура файлов

```
ethereum/
├── scripts/
│   ├── deploy.js                          # Основной скрипт деплоя
│   ├── check-balance.js                   # Проверка баланса
│   ├── verify-deployment.js               # Проверка деплоя
│   └── generate-verification-commands.js  # Генерация команд верификации
├── .env.example                           # Пример настроек
├── .env                                   # Ваши настройки (НЕ коммитить!)
├── deployment-info.json                   # Адреса контрактов (генерируется)
└── verify-commands.sh                     # Команды верификации (генерируется)
```

## Команды для деплоя

### Подготовка
```bash
# 1. Установка зависимостей
npm install

# 2. Настройка .env
cp .env.example .env
# Отредактируйте .env файл

# 3. Проверка баланса
npm run check-balance
```

### Деплой
```bash
# Компиляция
npm run compile

# Тесты (опционально)
npm test

# Деплой в Sepolia
npm run deploy:sepolia

# Проверка деплоя
npm run verify-deployment
```

### Верификация на Etherscan
```bash
# Генерация команд
node scripts/generate-verification-commands.js

# Запуск верификации
bash verify-commands.sh
```

## Что происходит при деплое?

### Порядок деплоя контрактов:

1. **TokenManager**
   - Управляет списком разрешенных токенов
   - Constructor: принимает адрес owner

2. **OrderBook** 
   - Хранит все ордера DEX
   - Constructor: принимает адрес TokenManager

3. **Trade**
   - Исполняет ордера из OrderBook
   - Constructor: принимает адрес OrderBook

4. **Связывание контрактов**
   - OrderBook.setTradeContract(Trade.address)

5. **Test Tokens** (опционально)
   - Два тестовых ERC20 токена для демонстрации

### Примерная стоимость:

| Операция | Gas | Стоимость (~20 gwei) |
|----------|-----|---------------------|
| TokenManager | ~250k | ~0.005 ETH |
| OrderBook | ~750k | ~0.015 ETH |
| Trade | ~500k | ~0.010 ETH |
| Set Trade | ~50k | ~0.001 ETH |
| Test Tokens | ~400k | ~0.008 ETH |
| **ИТОГО** | **~1.95M** | **~0.039 ETH** |

## Файлы после деплоя

### deployment-info.json
```json
{
  "network": "sepolia",
  "deployer": "0x...",
  "timestamp": "2026-01-13T...",
  "contracts": {
    "TokenManager": "0x...",
    "OrderBook": "0x...",
    "Trade": "0x...",
    "TestTokenA": "0x...",
    "TestTokenB": "0x..."
  }
}
```

## Проверка успешности деплоя

### ✅ Чеклист:
- [ ] Все 5 контрактов задеплоены
- [ ] deployment-info.json создан
- [ ] `npm run verify-deployment` показывает все ✅
- [ ] Контракты видны на https://sepolia.etherscan.io/
- [ ] Баланс deployer'а уменьшился на ~0.04 ETH

### Просмотр на Etherscan:
```
https://sepolia.etherscan.io/address/<CONTRACT_ADDRESS>
```

## Взаимодействие с контрактами

### Через Hardhat Console
```bash
npx hardhat console --network sepolia
```

```javascript
const info = require('./deployment-info.json');
const OrderBook = await ethers.getContractAt('OrderBook', info.contracts.OrderBook);

// Создание ордера
const tx = await OrderBook.createOrder(
  tokenA_address,
  tokenB_address,
  sellAmount,
  buyAmount
);
await tx.wait();
```

### Через Etherscan (Web UI)
1. Откройте контракт на Etherscan
2. Перейдите на "Write Contract"
3. Подключите кошелек
4. Вызовите нужную функцию

## Полезные команды

```bash
# Проверка сети
npx hardhat console --network sepolia
> network.name

# Проверка баланса
npm run check-balance

# Компиляция
npm run compile

# Очистка артефактов
npm run clean

# Тесты с gas report
npm run test:gas

# Coverage
npm run test:coverage
```

## Troubleshooting

### Не хватает ETH
```bash
# Получите с faucet
https://sepoliafaucet.com/
https://www.alchemy.com/faucets/ethereum-sepolia
```

### Ошибки nonce
```bash
# Очистите историю в MetaMask
Settings → Advanced → Clear activity tab data
```

### Контракт не верифицируется
```bash
# Подождите 1-2 минуты после деплоя
# Проверьте что передали правильные constructor аргументы
# Убедитесь что версия Solidity совпадает (0.8.20)
```

## Поддержка

- 📚 Документация Hardhat: https://hardhat.org/
- 🔍 Sepolia Etherscan: https://sepolia.etherscan.io/
- 💬 Alchemy Discord: https://discord.gg/alchemy
- 📖 OpenZeppelin Docs: https://docs.openzeppelin.com/

## Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте `.env` файл
- Храните приватные ключи в безопасности
- Используйте отдельный кошелек для тестовых сетей
- Проверяйте адреса перед отправкой транзакций

## Следующие шаги

После успешного деплоя вы можете:

1. ✅ Верифицировать контракты на Etherscan
2. 🎨 Создать фронтенд для взаимодействия
3. 🧪 Протестировать все функции через UI
4. 📊 Мониторить транзакции на Etherscan
5. 🚀 Деплой в mainnet (когда будете готовы)

---

**Создано:** 2026-01-13
**Версия контрактов:** 1.0.0
**Solidity:** 0.8.20
**Network:** Sepolia Testnet
